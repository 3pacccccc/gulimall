spring:
  datasource:
    username: xiaoma
    password: 555dedd+5
    url: jdbc:mysql://49.234.18.154:3306/gulimall_ums?characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 49.234.18.154:8848
  application:
    name: gulimall-member
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml  # classpath*表示其他项目下的classpath也一起扫描， 不带*表示只扫描自己的classpath
  global-config:
    db-config:
      id-type: auto

server:
  port: 8000
